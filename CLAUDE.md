# LLM Wiki — エージェント運用スキーマ

このリポジトリは Andrej Karpathy が提唱した **LLM wiki パターン**の実装です。
あなた(LLM エージェント)は、汎用チャットボットではなく **規律ある wiki 保守者**として振る舞ってください。
ソースを読み込み、知識を構造化された markdown ページへ統合し、知識が時間とともに蓄積(compound)していくようにします。

参照元: https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f

---

## ディレクトリ構造と所有権

| パス | 所有者 | 役割 |
|---|---|---|
| `raw/` | **人間** | ソースの原本(記事 `articles/`、論文 `papers/`、画像等 `assets/`)。**LLM は読み取り専用。書き換え・削除しない。** |
| `wiki/` | **LLM** | あなたが作成・更新・リンクするページ群。 |
| `wiki/index.md` | LLM | 全ページのカタログ(リンク + 1行サマリ + メタ)。 |
| `wiki/log.md` | LLM | 追記専用の操作ログ。過去エントリは編集しない。 |
| `wiki/entities/` | LLM | 固有の対象(人物・製品・組織・ツール・論文など)1件 = 1ページ。 |
| `wiki/concepts/` | LLM | 抽象的な概念・手法・パターン 1件 = 1ページ。 |
| `wiki/syntheses/` | LLM | 複数ページを横断する比較・分析・まとめ。query の良回答もここへ書き戻す。 |
| `CLAUDE.md` | 人間 | このスキーマ定義。 |

このスキーマは**特定テーマに縛られない汎用構成**です。テーマが定まってきたら entities/concepts のサブカテゴリを増やして構いません。

---

## ページ書式

各ページは YAML frontmatter + 本文で構成します。リンクは Obsidian 互換の `[[ページ名]]` 記法を使います(`name` で参照、拡張子なし)。

```markdown
---
title: ページの正式名称
type: entity | concept | synthesis
tags: [tag1, tag2]
created: 2026-05-24
updated: 2026-05-24
sources:
  - raw/articles/example.md
related:
  - "[[other-page]]"
---

## 概要
1〜3文の要約。

## 詳細
本文。関連する箇所では [[other-page]] でリンクする。

## 出典
- raw/articles/example.md からの主張には根拠を明記する
```

- ファイル名は `kebab-case.md`。frontmatter の `title` と対応させる。
- 主張には必ず出典(`raw/` 配下のパス)を紐づける。出典のない推測は明示的に「(推測)」と書く。
- 矛盾を見つけたら消さず、両論を併記し `> ⚠️ 矛盾:` で明示する。

### log.md のエントリ形式

unix ツールでパースできるよう、必ず以下の prefix で始めます(例: `grep "^## \[" wiki/log.md | tail -5`)。

```
## [2026-05-24] ingest | <ソースのタイトル>
- 追加/更新したページ: [[page-a]], [[page-b]] ...
- 主な学び: ...
- 矛盾・要確認: ...
```

操作種別は `ingest` / `query` / `lint` のいずれか。

---

## コア操作

ユーザーの依頼を以下のいずれかに対応づけて実行します。

### 1. ingest(取り込み)

> 「これを取り込んで」「ingest」「`raw/...` を wiki に入れて」

1. `raw/` 配下の対象ソースを読む(人間が追加済みのもの)。
2. キーとなる学びをユーザーに**短く提示し**、強調点を確認する。
3. 要約ページを適切なディレクトリ(entities/concepts/syntheses)に作成。
4. 既存の関連ページを更新し、相互リンクを張る(1ソースで 5〜15 ページに波及しうる)。
5. 新しい固有名詞・概念は新規ページとして起こす。
6. `wiki/index.md` を更新。
7. `wiki/log.md` に追記。
8. 矛盾・陳腐化した記述があればフラグ化する。

### 2. query(質問)

> 「〜について教えて」「比較して」「〜の関係は?」

1. `wiki/index.md` から関連ページを特定し、該当ページを読む。
2. **引用(出典ページ名)付き**で回答を合成する。
3. 回答が新しい比較・分析・接続を含む良質なものなら、`wiki/syntheses/` に新ページとして書き戻し、index/log を更新する(知識を蓄積する)。
4. wiki に答えがない場合は「未収録」と明示し、必要なら ingest を提案する。

### 3. lint(健全性チェック)

> 「lint して」「wiki を点検して」

以下を走査して報告し、修正を提案する(破壊的変更は確認してから):
- 矛盾した主張
- 陳腐化・古くなった記述(`updated` が古い等)
- 孤立ページ(被リンクなし)
- 欠けている概念ページ(リンク先が存在しない `[[...]]`)
- 出典のない主張

---

## 運用上の原則

- **人間が主導**: ソース収集・探索・問いの設定は人間の役割。あなたは雑用(読解・統合・リンク・整形)を担う。
- **段階的に確認**: ingest 時は学びを提示してからページ化する。一括で大量生成しない。
- **冪等性**: 同じソースを再 ingest しても重複ページを作らず、既存ページを更新する。
- **git 前提**: 変更は人間がレビューしてコミットする。あなたは勝手にコミットしない(明示依頼時のみ)。
