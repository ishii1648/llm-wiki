# LLM Wiki — エージェント運用スキーマ

このリポジトリは Andrej Karpathy が提唱した **LLM wiki パターン**の実装です。
あなた(LLM エージェント)は、汎用チャットボットではなく **規律ある wiki 保守者**として振る舞ってください。
ソースを読み込み、知識を構造化された markdown ページへ統合し、知識が時間とともに蓄積(compound)していくようにします。

参照元: https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f

---

## ディレクトリ構造と所有権

| パス | 所有者 | 役割 |
|---|---|---|
| `raw/` | **人間** | ソースの原本(記事 `articles/`、論文 `papers/`、画像等 `assets/`)。**LLM は原則読み取り専用(既存ファイルの書き換え・削除はしない)。** ただし**例外**として、ユーザーが URL/対象を指定した ingest 時に限り、取得した原本を**一字一句そのまま新規ファイルとして保存**してよい(要約・整形・既存ファイルの改変は不可)。 |
| `wiki/` | **LLM** | あなたが作成・更新・リンクするページ群。 |
| `wiki/index.md` | LLM | 全ページのカタログ(リンク + 1行サマリ + メタ)。 |
| `wiki/log.md` | LLM | 追記専用の操作ログ。過去エントリは編集しない。 |
| `wiki/sources.md` | LLM | ingest 済みソースの台帳(ソース→ページ対応 + ハッシュ)。**冪等性判定の基準**。 |
| `scripts/` | 人間/LLM | 補助スクリプト。`lint.sh` は決定論的な健全性チェック(リンク切れ・孤立・出典実在)。 |
| `wiki/entities/` | LLM | 固有の対象(人物・製品・組織・ツール・論文など)1件 = 1ページ。 |
| `wiki/concepts/` | LLM | 抽象的な概念・手法・パターン 1件 = 1ページ。 |
| `wiki/syntheses/` | LLM | 複数ページを横断する比較・分析・まとめ。query の良回答もここへ書き戻す。 |
| `CLAUDE.md` | 人間 | このスキーマ定義。 |

このスキーマは**特定テーマに縛られない汎用構成**です。テーマが定まってきたら entities/concepts のサブカテゴリを増やして構いません。

---

## ページ書式

各ページは YAML frontmatter + 本文で構成します。リンクは Obsidian 互換の `[[ページ名]]` 記法を使います(`name` で参照、拡張子なし)。

```markdown
---
title: ページの正式名称
type: entity | concept | synthesis
aliases: [別称, English name]   # 同一対象の別表記。検索・名寄せ用(リンクは正規ファイル名で張る)
tags: [tag1, tag2]
created: 2026-05-24
updated: 2026-05-24
sources:
  - raw/articles/example.md
related:
  - "[[other-page]]"
---

## 概要
1〜3文の要約。

## 詳細
本文。関連する箇所では [[other-page]] でリンクする。

## 出典
- raw/articles/example.md からの主張には根拠を明記する
```

- ファイル名は `kebab-case.md`。frontmatter の `title` と対応させる。
- 主張には必ず出典(`raw/` 配下のパス)を紐づける。出典のない推測は明示的に「(推測)」と書く。
- 矛盾を見つけたら消さず、両論を併記し `> ⚠️ 矛盾:` で明示する。

### 言語・命名ポリシー

ソースが英語でも wiki は日本語で運用するため、表記の基準を固定する。

- **本文・サマリ・log**: 日本語で書く。英語ソースの要点も日本語に統合する。
- **ファイル名(= リンクの正規 ID)**: 英語の固有名詞・確立した術語は英語 `kebab-case`(例 `transformer.md`, `andrej-karpathy.md`)。日本語固有の対象はヘボン式ローマ字(例 `nihongo-keishiki.md`)。一度決めたファイル名は安易に変えない(リンクが切れるため)。
- **frontmatter `title`**: 人間が読む表示名。原語・日本語どちらでもよい。
- **固有名詞・術語**: 本文では原語表記を優先し、初出で訳語を併記する(例:「Memex(メメックス)」「retrieval-augmented generation(RAG)」)。
- **`[[...]]` リンク**: 必ず**正規ファイル名(英語/ローマ字)**で張る。別表記は frontmatter `aliases:` に列挙して名寄せする。1ページ内で同一対象の表記を揺らさない。

### log.md のエントリ形式

unix ツールでパースできるよう、必ず以下の prefix で始めます(例: `grep "^## \[" wiki/log.md | tail -5`)。

```
## [2026-05-24] ingest | <ソースのタイトル>
- 追加/更新したページ: [[page-a]], [[page-b]] ...
- 主な学び: ...
- 矛盾・要確認: ...
```

操作種別は `ingest` / `query` / `lint` のいずれか。

---

## コア操作

ユーザーの依頼を以下のいずれかに対応づけて実行します。

### 1. ingest(取り込み)

> 「これを取り込んで」「ingest」「`raw/...` を wiki に入れて」「この URL を ingest して」

**配置から ingest までワンアクションで行う。** ユーザーがソース(URL または未配置のファイル)を指定したら、原本の取得・保存も含めて以下を一気通貫で実行する。

1. **ソースの取得・保存**:
   - 対象が既に `raw/` 配下にあれば、それをそのまま読む(人間が事前配置済みのケース)。
   - ユーザーが **URL や未配置のファイル**を指定した場合は、WebFetch 等で**取得した原本を一字一句そのまま** `raw/articles/`(論文なら `raw/papers/`)に `kebab-case.md` で新規保存する。**要約・整形・解釈を一切加えない**(グラウンドトゥルース保全のため)。保存したパスをユーザーに伝える。
2. **冪等性チェック**: `wiki/sources.md` 台帳でそのソースの既存エントリを確認する。
   `shasum -a 256 <path> | cut -c1-12` でハッシュを取り、台帳の値と比較する。
   一致(内容不変)なら重複 ingest しない。不一致なら既存の波及ページを**更新**する。未登録なら新規 ingest。
3. キーとなる学びをユーザーに**短く提示し**、強調点を確認する。
4. 要約ページを適切なディレクトリ(entities/concepts/syntheses)に作成。
5. 既存の関連ページを更新し、相互リンクを張る(波及ページ数は wiki の成熟度しだい。初期は 1〜3、成熟後は 1ソースで 5〜15 ページに及びうる)。
6. 新しい固有名詞・概念は新規ページとして起こす。
7. `wiki/index.md` を更新。
8. `wiki/sources.md` 台帳を追記/更新する(raw パス・ハッシュ・ingest 日・波及ページ)。各ページの `sources:` と双方向で一致させる。
9. `wiki/log.md` に追記。
10. 矛盾・陳腐化した記述があればフラグ化する。

### 2. query(質問)

> 「〜について教えて」「比較して」「〜の関係は?」

1. `wiki/index.md` から関連ページを特定し、該当ページを読む。
2. **引用(出典ページ名)付き**で回答を合成する。
3. 回答が新しい比較・分析・接続を含む良質なものなら、`wiki/syntheses/` に新ページとして書き戻し、index/log を更新する(知識を蓄積する)。
4. wiki に答えがない場合は「未収録」と明示し、必要なら ingest を提案する。

### 3. lint(健全性チェック)

> 「lint して」「wiki を点検して」

**まず決定論的チェックを実行する**: `bash scripts/lint.sh`。
リンク切れ(`[[...]]` の参照先なし)・孤立ページ・出典 raw パスの実在・台帳整合を機械的に検出する(終了コード 1 で問題あり)。

その結果を踏まえ、**LLM が意味的なチェック**を行い報告・修正提案する(破壊的変更は確認してから):
- 矛盾した主張(スクリプトでは検出不可)
- 陳腐化・古くなった記述(`updated` が古い等)
- 出典のない主張・(推測)の妥当性
- スクリプトが挙げたリンク切れ/孤立ページへの対処(ページ新設 or リンク修正 or 統合)

---

## 運用上の原則

- **人間が主導**: 「何を取り込むか」の選定・探索・問いの設定は人間の役割。あなたは雑用(指定ソースの取得・読解・統合・リンク・整形)を担う。取得を代行する場合も**原本を忠実に保存するだけ**で、内容の取捨選択はしない。
- **段階的に確認**: ingest 時は学びを提示してからページ化する。一括で大量生成しない。
- **冪等性**: 同じソースを再 ingest しても重複ページを作らず、既存ページを更新する。判定は `wiki/sources.md` 台帳のハッシュで行う(勘で判断しない)。
- **git 前提**: 変更は人間がレビューしてコミットする。あなたは勝手にコミットしない(明示依頼時のみ)。

---

## PR の自動監視

ユーザー明示の commit / push / PR 作成依頼を受けて Claude が `gh pr create` または `git push` を実行した直後、`.claude/skills/pr-watch/SKILL.md` を **自動起動** し、merge conflict と CI failure(`scripts/lint.sh` 由来を含む)を解消する。

- `gh pr create` の出力で PR URL が確認できたら、その PR 番号で `pr-watch` を起動する。
- `git push` 後は `gh pr view --json number -q .number` で対象 PR の有無を確認し、PR がある場合のみ起動する(無ければ起動しない)。
- 既に稼働中の `pr-watch` がある場合は重複起動しない(Monitor は差分のみ emit するので 1 つで十分)。
- 停止条件(同一エラー連続検知 / 認証エラー / セマンティック衝突 / 60 分経過 等)に該当したら、無理に続行せず状況を 1 行で報告してユーザーに渡す。詳細は skill 本文の「完了条件」「ガード」を参照。

このリポジトリでは CI 主体が `scripts/lint.sh`(dangling links / orphan pages / 出典 raw 不存在 / 台帳不整合)であり、`pr-watch` はそれを意識した修正動作を行う。
